<UserControl x:Class="Restaurant_SAP.Views.MesaPedidoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Restaurant_SAP.Views"
             xmlns:viewmodel="clr-namespace:Restaurant_SAP.ViewModels"
             xmlns:models="clr-namespace:Restaurant_SAP.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox  Grid.Row="0">
            <ListBox ItemsSource="{Binding Mesas}" SelectedItem="{Binding SelectedMesa}" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Numero}" FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Descripcion}" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Estado}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <GroupBox Grid.Row="1">
            <ListBox ItemsSource="{Binding Pedidos}" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Mesa.Numero}" FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Menu.Nombre}" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Menu.Descripcion}"/>
                            <TextBlock Text="{Binding Estado}"/>
                            <TextBlock Text="{Binding FechaHora}"/>
                            <TextBlock Text="{Binding Precio}"/>
                            <TextBlock Text="{Binding Cantidad}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="{Binding SelectedMesa.Numero}" FontWeight="Bold" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding SelectedMesa.Descripcion}" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding SelectedMesa.Estado}"/>

            <Button x:Name="abrirPedido" Content="Agregar Pedido" 
                Command="{Binding AbrirModalPedidoCommand}" Margin="5" ></Button>
        </StackPanel>

        <Popup x:Name="popupAgregarPedido" IsOpen="{Binding IsModalAbierto}" 
               PlacementTarget="{Binding ElementName=btnAbrirModal}" Placement="Center" Width="300" Height="200">
            <Border Background="White" BorderBrush="Black" BorderThickness="1">
                <local:PedidoAddView_Modal DataContext="{Binding}" />
            </Border>
        </Popup>
    </Grid>
</UserControl>
