<UserControl x:Class="Restaurant_SAP.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Restaurant_SAP.Views"
             xmlns:viewmodels="clr-namespace:Restaurant_SAP.ViewModels"
             xmlns:p="clr-namespace:Restaurant_SAP.Properties"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="200">
    <Grid>
        <StackPanel>
            <TextBlock Text="{x:Static p:Resources.TextBlock_SeleccionarIdioma}" Margin="5"/>
            <ComboBox ItemsSource="{Binding IdiomasDisponibles}"
                      SelectedItem="{Binding IdiomaSeleccionado, UpdateSourceTrigger=PropertyChanged}"
                      DisplayMemberPath="DisplayName"
                      Margin="5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button Content="{x:Static p:Resources.Button_Guardar}"
                        Command="{Binding GuardarIdiomaCommand}"
                        Margin="5"/>
                <Button Content="{x:Static p:Resources.Button_Cancelar}"
                        Command="{Binding CerrarModalCommand}"
                        Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>