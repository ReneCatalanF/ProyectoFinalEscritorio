<UserControl x:Class="Restaurant_SAP.Views.HistorialEconomicoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Restaurant_SAP.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox ItemsSource="{Binding Pedidos}" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding FechaHora}" FontWeight="Bold" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Menu.Nombre}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Menu.Descripcion}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Mesa.Numero}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Precio}" Margin="0,0,5,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
