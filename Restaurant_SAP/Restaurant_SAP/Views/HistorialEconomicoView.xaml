<UserControl x:Class="Restaurant_SAP.Views.HistorialEconomicoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Restaurant_SAP.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0" ItemsSource="{Binding Pedidos}" Margin="10" HorizontalAlignment="Stretch">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Fecha/Hora" DisplayMemberBinding="{Binding FechaHora, StringFormat=dd/MM/yyyy HH:mm}" Width="Auto" />
                    <GridViewColumn Header="Nombre Menú" DisplayMemberBinding="{Binding Menu.Nombre}" Width="Auto" />
                    <GridViewColumn Header="Descripción Menú" DisplayMemberBinding="{Binding Menu.Descripcion}" Width="400" />
                    <GridViewColumn Header="Nº Mesa" DisplayMemberBinding="{Binding Mesa.Numero}" Width="Auto" />
                    <GridViewColumn Header="Precio" DisplayMemberBinding="{Binding Precio, StringFormat=C}" Width="Auto" />
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="2"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1,0,0,0" Padding="5" Background="#EEEEEE">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Total:" HorizontalAlignment="Right" FontWeight="Bold" Margin="5" />
                <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat=C}" HorizontalAlignment="Right" Margin="5" FontWeight="Bold" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
